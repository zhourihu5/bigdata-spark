<?xml version="1.0" encoding="utf-8"?>

<coordinator-app xmlns="uri:oozie:coordinator:0.4" name="cron_ads_etl" frequency="${freq}" start="${start}" end="${end}" timezone="Asia/Shanghai">
    <action>
        <workflow>
            <app-path>${appPath}</app-path>
        </workflow>
    </action>
</coordinator-app>